

vector $joint=`xform -q -ws -t R_thumb_1`;
select -r R_thumb_ctl_1 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_ctl_1.scalePivot R_thumb_ctl_1.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_thumb_ctl_1 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_thumb_roll_1_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_thumb_1`;
select -r R_thumb_roll_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_roll_1_group.scalePivot R_thumb_roll_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_thumb_ctl_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_ctl_1_group.scalePivot R_thumb_ctl_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;





vector $joint=`xform -q -ws -t R_thumb_2`;
select -r R_thumb_ctl_2 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_ctl_2.scalePivot R_thumb_ctl_2.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_thumb_ctl_2 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_thumb_roll_2_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_thumb_2`;
select -r R_thumb_roll_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_roll_2_group.scalePivot R_thumb_roll_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_thumb_ctl_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_ctl_2_group.scalePivot R_thumb_ctl_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;




vector $joint=`xform -q -ws -t R_thumb_3`;
select -r R_thumb_ctl_3 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_ctl_3.scalePivot R_thumb_ctl_3.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_thumb_ctl_3 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_thumb_roll_3_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_thumb_3`;
select -r R_thumb_roll_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_roll_3_group.scalePivot R_thumb_roll_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_thumb_ctl_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_thumb_ctl_3_group.scalePivot R_thumb_ctl_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;





























vector $joint=`xform -q -ws -t R_index_1`;
select -r R_index_ctl_1 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_ctl_1.scalePivot R_index_ctl_1.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_index_ctl_1 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_index_roll_1_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_index_1`;
select -r R_index_roll_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_roll_1_group.scalePivot R_index_roll_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_index_ctl_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_ctl_1_group.scalePivot R_index_ctl_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;





vector $joint=`xform -q -ws -t R_index_2`;
select -r R_index_ctl_2 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_ctl_2.scalePivot R_index_ctl_2.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_index_ctl_2 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_index_roll_2_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_index_2`;
select -r R_index_roll_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_roll_2_group.scalePivot R_index_roll_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_index_ctl_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_ctl_2_group.scalePivot R_index_ctl_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;




vector $joint=`xform -q -ws -t R_index_3`;
select -r R_index_ctl_3 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_ctl_3.scalePivot R_index_ctl_3.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_index_ctl_3 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_index_roll_3_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_index_3`;
select -r R_index_roll_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_roll_3_group.scalePivot R_index_roll_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_index_ctl_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_index_ctl_3_group.scalePivot R_index_ctl_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;











































vector $joint=`xform -q -ws -t R_middle_1`;
select -r R_middle_ctl_1 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_ctl_1.scalePivot R_middle_ctl_1.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_middle_ctl_1 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_middle_roll_1_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_middle_1`;
select -r R_middle_roll_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_roll_1_group.scalePivot R_middle_roll_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_middle_ctl_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_ctl_1_group.scalePivot R_middle_ctl_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;





vector $joint=`xform -q -ws -t R_middle_2`;
select -r R_middle_ctl_2 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_ctl_2.scalePivot R_middle_ctl_2.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_middle_ctl_2 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_middle_roll_2_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_middle_2`;
select -r R_middle_roll_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_roll_2_group.scalePivot R_middle_roll_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_middle_ctl_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_ctl_2_group.scalePivot R_middle_ctl_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;




vector $joint=`xform -q -ws -t R_middle_3`;
select -r R_middle_ctl_3 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_ctl_3.scalePivot R_middle_ctl_3.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_middle_ctl_3 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_middle_roll_3_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_middle_3`;
select -r R_middle_roll_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_roll_3_group.scalePivot R_middle_roll_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_middle_ctl_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_middle_ctl_3_group.scalePivot R_middle_ctl_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;



















vector $joint=`xform -q -ws -t R_ring_1`;
select -r R_ring_ctl_1 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_ctl_1.scalePivot R_ring_ctl_1.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_ring_ctl_1 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_ring_roll_1_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_ring_1`;
select -r R_ring_roll_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_roll_1_group.scalePivot R_ring_roll_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_ring_ctl_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_ctl_1_group.scalePivot R_ring_ctl_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;





vector $joint=`xform -q -ws -t R_ring_2`;
select -r R_ring_ctl_2 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_ctl_2.scalePivot R_ring_ctl_2.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_ring_ctl_2 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_ring_roll_2_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_ring_2`;
select -r R_ring_roll_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_roll_2_group.scalePivot R_ring_roll_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_ring_ctl_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_ctl_2_group.scalePivot R_ring_ctl_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;




vector $joint=`xform -q -ws -t R_ring_3`;
select -r R_ring_ctl_3 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_ctl_3.scalePivot R_ring_ctl_3.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_ring_ctl_3 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_ring_roll_3_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_ring_3`;
select -r R_ring_roll_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_roll_3_group.scalePivot R_ring_roll_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_ring_ctl_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_ring_ctl_3_group.scalePivot R_ring_ctl_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;


















vector $joint=`xform -q -ws -t R_pinky_1`;
select -r R_pinky_ctl_1 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_ctl_1.scalePivot R_pinky_ctl_1.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_pinky_ctl_1 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_pinky_roll_1_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_pinky_1`;
select -r R_pinky_roll_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_roll_1_group.scalePivot R_pinky_roll_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_pinky_ctl_1_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_ctl_1_group.scalePivot R_pinky_ctl_1_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;





vector $joint=`xform -q -ws -t R_pinky_2`;
select -r R_pinky_ctl_2 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_ctl_2.scalePivot R_pinky_ctl_2.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_pinky_ctl_2 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_pinky_roll_2_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_pinky_2`;
select -r R_pinky_roll_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_roll_2_group.scalePivot R_pinky_roll_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_pinky_ctl_2_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_ctl_2_group.scalePivot R_pinky_ctl_2_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;




vector $joint=`xform -q -ws -t R_pinky_3`;
select -r R_pinky_ctl_3 ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_ctl_3.scalePivot R_pinky_ctl_3.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r R_pinky_ctl_3 ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ("R_pinky_roll_3_group");
rename "group1" $ctrlGroup;

vector $joint=`xform -q -ws -t R_pinky_3`;
select -r R_pinky_roll_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_roll_3_group.scalePivot R_pinky_roll_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;

select -r $ctrlName[0] ;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;

select -r R_pinky_ctl_3_group ;
SnapToPoint; dR_enterForSnap;
move -rpr ($joint.x) ($joint.y) ($joint.z) R_pinky_ctl_3_group.scalePivot R_pinky_ctl_3_group.rotatePivot ;
SnapToPoint; dR_enterForSnap;































select -r R_thumb_ctl_1_group ;
string $ctrlName[] = `ls -sl`;
doGroup 0 1 1;
print ($ctrlName);
$ctrlGroup = ($ctrlName[0] + "_group");
rename "group1" $ctrlGroup;